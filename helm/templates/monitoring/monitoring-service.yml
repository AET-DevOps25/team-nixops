apiVersion: v1
kind: Service
metadata:
  name: monitoring-service
  namespace: "{{ .Values.MonitoringNamespace }}"
spec:
  type: ClusterIP
  selector:
    app: monitoring-selector
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
