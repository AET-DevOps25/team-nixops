apiVersion: v1
kind: Service
metadata:
  name: {{ include "base-genai.fullname" . }}
  labels:
    {{- include "base-genai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "base-genai.selectorLabels" . | nindent 4 }}
